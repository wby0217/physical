import React, { Component } from 'react';import {    Keyboard,    View,} from 'react-native';import ScrollableTabView from 'react-native-scrollable-tab-view';import DefaultTabBar from './defaultTabBar';import ScanQrCode from './scanQrCode';import SubmitPayInfo from './submitPayInfo';import { showToast, ErrorHandle, Header } from '../../../mesosphere';export default class FriendPay extends Component {    static navigationOptions = {            header: null,        }    constructor(props) {        super(props);        this.params = props.navigation.state.params;        this.navigation = props.navigation;        this.headerTitle = this.navigation.state.params.rechargeInfo.name;        console.log('FriendPay params', this.params );    }    render () {        const { navigation } = this.props;        return (            <View style={{flex:1}}>                <Header headerTitle={this.params.rechargeInfo.name} navigation = {navigation}/>                <ScrollableTabView                    renderTabBar={() => <DefaultTabBar/>}                    tabBarUnderlineStyle={{ backgroundColor: '#17A84B',height: 2 }}                    tabBarActiveTextColor="#17A84B"                    tabBarInactiveTextColor="#2D2D2D"                    tabBarBackgroundColor="#fff"                    onChangeTab = {Keyboard.dismiss}                    initialPage={0}>                    <ScanQrCode  tabLabel="扫码支付" params={this.params } />                    <SubmitPayInfo tabLabel="已支付，我要提单" params={this.params } navigation={navigation}/>                </ScrollableTabView>            </View>        )    }}
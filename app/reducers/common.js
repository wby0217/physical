import { combineReducers } from 'redux';import types from '../config/constants';function bankList (state = {}, action) {    let nextState;    switch (action.type) {        case types.saveBankList:            nextState = action.filter;            break;        default:            nextState = state;    }    return nextState || state;}export default combineReducers({    bankList})